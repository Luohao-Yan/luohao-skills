# tech-training-deck 依赖 slide-maker skill(提供 deckkit/anim/render_deck/lint_deck)。
# 本 skill 自身只需读 .pptx 与解析主题;渲染/lint 由 slide-maker 提供。
# 列在这里供 check_env.py 预检 + 同事 pip install 参考。

python-pptx>=1.0.0      # 读 .pptx 模板主题色/字体/版式(inspect_and_profile.py)
PyYAML>=6.0            # 读写 profile.yaml
Pillow>=10.0.0         # 图像度量(load_profile 配色对比校验可选)

# 渲染与 lint 由 slide-maker skill 提供,需其依赖(此处仅声明,由 slide-maker 的 requirements.txt 落地):
#   PyMuPDF, matplotlib, numpy
# 外部工具(由 slide-maker check_env 探测):
#   LibreOffice (soffice) — PNG 渲染必需
