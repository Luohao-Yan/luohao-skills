# editing-office-docs 依赖。
# 列在这里供同事 pip install 参考;docx_tools.py 直接 import,无 check_env 预检脚本。

python-docx>=1.0.0      # 读写 .docx(OOXML)、锚点插入、高亮、提取大纲
# Pillow 可选:仅当需要读取/缩放内嵌图片(如核对 E-R 图)时用
# Pillow>=10.0.0

# 外部工具(非 pip,需单独安装):
#   LibreOffice (soffice) — .doc<->.docx 互转必需;纯 .docx 编辑无需
